# Live competitor creatives

Images + copy pulled from Meta Ad Library for the Competitor Wall.

- `live-snapshots.json` — primary text, headlines, descriptions, CTAs, Library IDs
- `{brand}-{n}.jpg` — creative stills

Designers: you don’t need to gather these. Browse the wall, mock a take, pitch George.

Refresh: scheduled Cursor Automation every ~2–3 weeks (or run a scrape + `npm run generate:competitors`).
